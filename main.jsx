import React from "react";
import ReactDOM from "react-dom/client";
import AutoRealm from "./AutoRealm.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AutoRealm />
  </React.StrictMode>
);
